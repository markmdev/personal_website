---
import { Image } from "astro:assets";

const { src, alt, label } = Astro.props;
---

<div class="flex flex-col items-center w-16 text-center">
  <Image src={src} alt={alt} class="w-6 h-auto" loading="eager" />
  <span class="mt-0.5 text-[10px] sm:text-xs leading-tight">{label}</span>
</div>
